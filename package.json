{
  "name": "markdown-editor-monorepo",
  "version": "1.0.0",
  "description": "Markdown Editor with AI Agent - Monorepo",
  "private": true,
  "workspaces": ["frontend", "backend"],
  "scripts": {
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix backend",
    "start": "powershell -File ./start-all.ps1",
    "stop": "powershell -File ./stop-all.ps1"
  }
}
